<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="per.stock.mapper.KLineMapper">

    <select id="queryLastDate" resultType="java.lang.String">
        select max(trans_date) from k_line_day where code = #{stockCode}
    </select>

    <insert id="batchAddDayK" parameterType="per.stock.bean.KLineBean">
        insert into k_line_day(
            code, trans_date, open_price, close_price, high_price, 
            low_price, volume, amount, create_time, modify_time)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.code}, #{item.transDate,jdbcType=DATE}, #{item.openPrice,jdbcType=DECIMAL}, #{item.closePrice,jdbcType=DECIMAL}, #{item.highPrice,jdbcType=DECIMAL},
            #{item.lowPrice,jdbcType=DECIMAL}, #{item.volume}, #{item.amount}, now(), now()
            )
        </foreach>
    </insert>

    <insert id="addWeekK" parameterType="per.stock.bean.KLineBean">
        insert into k_line_day(
        code, trans_date, open_price, close_price, high_price,
        low_price, volume, amount, create_time, modify_time)
        values (
        #{code}, #{transDate,jdbcType=DATE}, #{openPrice,jdbcType=DECIMAL}, #{closePrice,jdbcType=DECIMAL}, #{highPrice,jdbcType=DECIMAL},
        #{lowPrice,jdbcType=DECIMAL}, #{volume}, #{amount}, now(), now()
        )
    </insert>

    <insert id="addMonthK" parameterType="per.stock.bean.KLineBean">
        insert into k_line_day(
        code, trans_date, open_price, close_price, high_price,
        low_price, volume, amount, create_time, modify_time)
        values (
        #{code}, #{transDate,jdbcType=DATE}, #{openPrice,jdbcType=DECIMAL}, #{closePrice,jdbcType=DECIMAL}, #{highPrice,jdbcType=DECIMAL},
        #{lowPrice,jdbcType=DECIMAL}, #{volume}, #{amount}, now(), now()
        )
    </insert>

    <select id="countByPrimaryKey" resultType="int">
        select count(*) from k_line_day where code = #{code} and  trans_date = #{transDate}
    </select>

    <select id="queryDayKAllTransDate" resultType="java.lang.String">
        select trans_date from k_line_day where code = #{code}
    </select>

</mapper>